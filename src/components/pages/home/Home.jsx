import "./home.css";

export const Home = () => {
	// todo lo que va antes del return es JS
	return (
		// a partir del return es lo que se va a a mostrar en el DOM (HTML)
		// podemos interpolar el JS declarado antes del return se pone entre {}
		<div className="h-conta">
			<h3 className="titulo">algo</h3>
		</div>
	);
};
